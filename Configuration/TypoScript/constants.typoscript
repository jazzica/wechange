plugin.tx_wechange {
    view {
        # cat=wechange: templates/file; type=string; label=Path to template root (FE)
        templateRootPath =
        # cat=wechange: templates/file; type=string; label=Path to template partials (FE)
        partialRootPath =
        # cat=wechange: templates/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }

    settings {
        map {
            # cat=wechange: config/map; type=string; label=Base URL for the WECHANGE Map
            baseUrl = https://wechange.de/
        }

        api {
            # cat=wechange: config/api; type=string; label=Base URL for the WECHANGE API
            baseUrl = https://wechange.de/api/v2/
        }
    }
}